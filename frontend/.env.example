# Frontend environment (copy to .env.local)

# true = use frontend-only mock responses (default for parallel development)
# false = call the real FastAPI backend
NEXT_PUBLIC_USE_MOCK_API=true

# Base URL for the FastAPI service (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000
