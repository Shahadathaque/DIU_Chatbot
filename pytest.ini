[pytest]
addopts = -m "not integration"
markers =
    integration: requires restored private DIU data/evaluation artifacts
    requires_artifacts(*names): names the private artifacts required by an integration test
